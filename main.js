const prompt = require('prompt-sync') ({ sigint: true});

const question = prompt('Please select a number. ');


function getAverage(n1, n2){
    const sum = n1 + n2;
    const average = sum / 2;
    return getAverage;
}